{
  "ConnectionStrings": {
    "Windows": "Connection String to local sqlexpress instance",
    "Mac": "connection string to local sqlexpress instance running on docker"
  },
  "AppSettings": {
    "Secret": "Encryption key for passwords",
    "DatabaseBackupDestination": "Database backup location"
  },
  "Jobs": {
    "ExportDatabaseJob": "0 30 0 ? * * *",
    "DailyCleanupJob": "59 59 23 ? * * *"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Warning"
    }
  },
  "AllowedHosts": "*"
}
